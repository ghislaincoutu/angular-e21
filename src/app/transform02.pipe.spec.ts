import { Transform02Pipe } from './transform02.pipe';

describe('Transform02Pipe', () => {
  it('create an instance', () => {
    const pipe = new Transform02Pipe();
    expect(pipe).toBeTruthy();
  });
});
